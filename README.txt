[Python 3.10]
- Ensure that python is 3.10

[Libraries]
Ensure that the libaries state below are installed. Command is shown below:
- pip install PyQt5
- pip install Pycryptodome
- pip install cryptography
- pip install urllib3
- pip install tqdm

[Steps]
Step 1: Import android apk or a directory of JAVA files
Step 2: Import keystore if you are inputting a android apk. If no file is detect, it will load a default keystore. Skip this step if you
are select/input a directory of JAVA files.(Optional)
Step 3: Select the obfuscation techniques
Step 4: Click Obfuscation button.

[Things to note]
After each successfuly obfuscation process, the software need to be restarted.